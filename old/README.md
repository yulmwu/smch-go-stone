# GO STONE

JavaFX와 JFoenix를 사용한 실시간 채팅 애플리케이션입니다.

## 📋 목차
- [프로젝트 소개](#-프로젝트-소개)
- [주요 기능](#-주요-기능)
- [기술 스택](#-기술-스택)
- [실행 방법](#-실행-방법)
  - [Maven으로 실행 (권장)](#1-maven으로-실행-권장)
  - [Eclipse에서 실행](#2-eclipse에서-실행)
- [데이터베이스 설정](#-데이터베이스-설정)
- [프로젝트 구조](#-프로젝트-구조)
- [화면 구성](#-화면-구성)

## 📖 프로젝트 소개

GO STONE은 JavaFX 기반의 GUI 채팅 애플리케이션으로, Oracle 데이터베이스를 활용하여 사용자 인증 및 실시간 채팅 기능을 제공합니다.

## ✨ 주요 기능

- **사용자 인증**
  - 회원가입 및 로그인
  - 비밀번호 찾기 및 재설정
  - 입력 유효성 검증

- **실시간 채팅**
  - 멀티유저 채팅 룸
  - 실시간 메시지 전송 및 수신
  - 채팅 기록 저장

- **Material Design UI**
  - JFoenix 라이브러리를 활용한 모던한 UI
  - 사용자 친화적인 인터페이스

## 🛠 기술 스택

- **언어**: Java 11+
- **UI 프레임워크**: JavaFX 11
- **UI 라이브러리**: JFoenix 8.0.10
- **데이터베이스**: Oracle Database (ojdbc6)
- **빌드 도구**: Maven

## 🚀 실행 방법

### 1. Maven으로 실행 (권장)

#### 사전 요구사항
- Java JDK 11 이상
- Maven 3.6 이상

#### 실행 단계

```bash
# 프로젝트 클론 또는 다운로드 후

# 1. 의존성 설치 및 빌드
mvn clean install

# 2. 애플리케이션 실행
mvn javafx:run

# 또는 JAR 파일로 패키징하여 실행
mvn clean package
java -jar target/go-stone-1.0.0.jar
```

### 2. Eclipse에서 실행

#### 사전 요구사항
- Eclipse IDE
- Java JDK 11 이상

#### 실행 단계

1. **프로젝트 열기**
   - Eclipse에서 `__GO STONE/eclipse-workspace` 열기

2. **외부 라이브러리 추가**
   - Package Explorer에서 `Go Stone` 우클릭
   - Properties > Java Build Path > Add External JARs...
   - `__GO STONE` 폴더의 JAR 파일 2개 추가
     - `jfoenix-8.0.10.jar`
     - `ojdbc6.jar`
   - Apply and Close

3. **JavaFX 플러그인 설치**
   - Help > Eclipse Marketplace
   - Find에 `javafx` 검색
   - `e(fx)clipse 3.6.0` 설치
   - Eclipse 재시작

4. **프로그램 실행**
   - `F11` 키를 눌러 실행

## 💾 데이터베이스 설정

### DB 연결 정보

기본 연결 정보는 `Controller.java`에 설정되어 있습니다:

```java
private String url = "jdbc:oracle:thin:@219.241.55.188:1521:xe";
private String user = "scott";
private String password = "1231";
```

### 테이블 생성

`src/DB.sql` 파일을 Oracle 데이터베이스에서 실행하여 필요한 테이블을 생성합니다:

1. **ACCOUNT 테이블**: 사용자 계정 정보
2. **CHAT 테이블**: 채팅 메시지 저장

```sql
-- 시퀀스 생성
create sequence account_seq start with 1 increment by 1 minvalue 1 nocycle;
create sequence chat_seq start with 1 increment by 1 minvalue 1 nocycle;

-- 테이블 생성
create table account (
    account_num number(20) primary key,
    id varchar2(100) not null,
    password varchar2(100) not null,
    realname varchar2(100) not null,
    nickname varchar2(100) not null,
    email varchar2(100) not null
);

create table chat (
    chat_num number(20) primary key,
    id varchar2(100) not null,
    nickname varchar2(100) not null,
    chat_time varchar2(100) not null,
    chat_text varchar2(100) not null
);
```

## 📁 프로젝트 구조

```
1-2. 20124 함알렉산더/
├── __GO STONE/
│   ├── eclipse-workspace/
│   │   └── Go Stone/
│   │       ├── src/
│   │       │   ├── application/
│   │       │   │   ├── Main.java           # 메인 애플리케이션 클래스
│   │       │   │   ├── Controller.java     # UI 컨트롤러 및 비즈니스 로직
│   │       │   │   ├── ChatThread.java     # 채팅 쓰레드
│   │       │   │   └── application.css     # 스타일시트
│   │       │   ├── fxml/
│   │       │   │   ├── login.fxml          # 로그인 화면
│   │       │   │   ├── main.fxml           # 메인 채팅 화면
│   │       │   │   ├── createAccount.fxml  # 회원가입 화면
│   │       │   │   ├── findPwd.fxml        # 비밀번호 찾기 화면
│   │       │   │   └── error.fxml          # 에러 다이얼로그
│   │       │   └── DB.sql                  # 데이터베이스 스크립트
│   │       └── bin/                        # 컴파일된 클래스 파일
│   ├── jfoenix-8.0.10.jar
│   └── ojdbc6.jar
├── pom.xml                                 # Maven 설정 파일
├── .gitignore                              # Git 무시 파일
├── README.md                               # 프로젝트 설명서
└── 실행방법.txt                            # 간단 실행 가이드
```

## 🖼 화면 구성

### 1. 로그인 화면
- ID/Password 입력
- 회원가입 이동
- 비밀번호 찾기

### 2. 회원가입 화면
- ID, Password, Real Name, Nickname, E-mail 입력
- 입력 유효성 검증
- 중복 확인

### 3. 비밀번호 찾기
- ID와 E-mail 확인
- 새 비밀번호 설정

### 4. 메인 채팅 화면
- 실시간 채팅 메시지 표시
- 메시지 전송

## 🔒 입력 유효성 검증

- **ID**: 영문, 숫자, 4~20자리
- **Password**: 영문, 숫자, 특수문자, 4~20자리
- **Real Name**: 영문, 한글, 1~50자리
- **Nickname**: 영문, 한글, 숫자, 1~10자리
- **E-mail**: 1~50자리

## 📝 참고사항

- 이 프로젝트는 교육 목적으로 제작되었습니다.
- SQL Injection 방지를 위해 PreparedStatement 사용을 권장합니다.
- 실제 배포 시에는 보안 강화가 필요합니다.

## 📄 라이선스

교육용 프로젝트

---

**개발자**: 20124 함알렉산더  
**마지막 업데이트**: 2025년 10월

